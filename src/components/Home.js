import React from 'react'

const Home = () => {
  const imgStyle = {
    marginTop: 50,
    width: 500,
    textAlign: 'center'
  }

  return <img style={imgStyle} src='/assets/codelife.png' />
}

export default Home
